# Threshold Decisions

A threshold decision connects forecast output to action.

Examples:
- act if risk probability exceeds tolerance
- escalate if threshold breach probability is high
- retrain if model drift probability crosses review level
- invest if demand probability exceeds capacity threshold
- gather more evidence if forecast uncertainty is too wide

Thresholds should reflect consequences, not probability alone.
