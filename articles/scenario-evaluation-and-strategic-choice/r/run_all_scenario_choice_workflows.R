# run_all_scenario_choice_workflows.R
# Run all R workflows for Scenario Evaluation and Strategic Choice.

args <- commandArgs(trailingOnly = FALSE)
file_arg <- grep("^--file=", args, value = TRUE)

if (length(file_arg) > 0) {
  script_path <- normalizePath(sub("^--file=", "", file_arg[1]), mustWork = TRUE)
  article_root <- normalizePath(file.path(dirname(script_path), ".."), mustWork = TRUE)
} else {
  article_root <- getwd()
}

r_dir <- file.path(article_root, "r")

scripts <- c(
  "scenario_evaluation_strategic_choice_workflow.R",
  "strategy_profiles.R",
  "regret_tables.R",
  "threshold_review_tables.R",
  "robustness_summary.R"
)

for (script in scripts) {
  path <- file.path(r_dir, script)
  message("Running ", path)
  source(path)
}

message("All R scenario-choice workflows completed.")
