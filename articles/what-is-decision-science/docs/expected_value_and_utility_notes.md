# Expected Value and Expected Utility Notes

Expected value evaluates alternatives by probability-weighted outcomes.

Expected utility extends this logic by applying a utility function to outcomes. This allows the model to represent risk attitudes, diminishing marginal value, or preference structures.

Expected value is useful when probabilities and outcomes are credible. It becomes fragile when probabilities are speculative, outcomes are incomplete, or stakeholder values cannot responsibly be represented by a single metric.
