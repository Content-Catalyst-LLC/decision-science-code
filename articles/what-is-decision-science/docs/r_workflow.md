# R Workflow

The R workflow reads Python-generated outputs and produces reproducible summaries, figures, regret profiles, robustness comparisons, and sensitivity diagnostics.

Run:

```bash
Rscript r/run_all_decision_science_workflows.R
```
